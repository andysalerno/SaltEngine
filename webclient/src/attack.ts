import { CardDrawn } from "./message";
import { logGameMessage } from "./util";

export function cardAttacksTarget(attacker: CardDrawn, target: CardDrawn) {
    logGameMessage("Attacking logic triggered.");
}